// Checking if a number is big

const check = function (number) {
if (number > 100) {
return "TRUE"
}
return "FALSE";
};

const result = check(101);
console.log(result);

// Bouncer at the club

const bouncerCheck = function (maxPersons, currentPersons, age) {
if (maxPersons > 100)
}

